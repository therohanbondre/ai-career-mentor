# legacy-peer-deps: required because next-auth@5 beta and @auth/prisma-adapter
# have peer dependency declarations that don't perfectly align with React 19.
# Without this flag, npm install fails with ERESOLVE on peer conflicts.
legacy-peer-deps=true
